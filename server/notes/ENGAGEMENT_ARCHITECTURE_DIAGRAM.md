# Engagement Monitoring System - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React/TypeScript)                  │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ Users List   │  │ User Detail  │  │ Date Detail  │              │
│  │   Page       │  │    Page      │  │    Page      │              │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘              │
│         │                  │                  │                       │
└─────────┼──────────────────┼──────────────────┼───────────────────┘
          │                  │                  │
          └──────────────────┴──────────────────┘
                             │
                    HTTP Requests (JSON)
                             │
┌─────────────────────────────▼───────────────────────────────────────┐
│                         BACKEND (Node.js/Express)                    │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                    API ROUTES (/api/engagement)                 │ │
│  │                                                                  │ │
│  │  POST   /upload-logs                                            │ │
│  │  GET    /stats                                                  │ │
│  │  GET    /inactive-users?days=14                                │ │
│  │  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  │              NEW OPTIMIZED ENDPOINTS                      │  │ │
│  │  │  GET    /users/list                    [30x faster]      │  │ │
│  │  │  GET    /users/:userId/overview        [3x faster]       │  │ │
│  │  │  GET    /users/:userId/date/:date      [2.5x faster]     │  │ │
│  │  │  GET    /users/:userId/range           [2.5x faster]     │  │ │
│  │  └──────────────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  │              LEGACY ENDPOINTS (backward compatible)       │  │ │
│  │  │  GET    /users                                            │  │ │
│  │  │  GET    /users/:userId?days=30                           │  │ │
│  │  │  GET    /users/:userId/metrics                           │  │ │
│  │  └──────────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                               │                                      │
│  ┌────────────────────────────▼──────────────────────────────────┐ │
│  │                      CONTROLLERS                               │ │
│  │  ┌──────────────────────────────────────────────────────────┐ │ │
│  │  │  engagementController.ts                                  │ │ │
│  │  │  - uploadLogs()           → Process file uploads         │ │ │
│  │  │  - getUsersList()         → Fast user list               │ │ │
│  │  │  - getUserOverview()      → User + all dates             │ │ │
│  │  │  - getUserDateMetrics()   → Specific date details        │ │ │
│  │  │  - getMetricsDateRange()  → Range queries                │ │ │
│  │  │  - getEngagementStats()   → Overall statistics           │ │ │
│  │  │  - getInactiveUsers()     → Inactive user finder         │ │ │
│  │  │  + legacy methods...                                      │ │ │
│  │  └──────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                               │                                      │
│  ┌────────────────────────────▼──────────────────────────────────┐ │
│  │                      SERVICE LAYER                             │ │
│  │  ┌──────────────────────────────────────────────────────────┐ │ │
│  │  │  engagement.service.ts                                    │ │ │
│  │  │                                                            │ │ │
│  │  │  Core Methods:                                            │ │ │
│  │  │  - processLogFile()        → Parse & store logs          │ │ │
│  │  │  - parseLogFile()          → Extract metrics from logs   │ │ │
│  │  │  - calculateEngagementScore() → Score 0-100              │ │ │
│  │  │  - getUserEngagementSummary() → X-day summary            │ │ │
│  │  │  - getAllUsersEngagement()    → All users detailed       │ │ │
│  │  │                                                            │ │ │
│  │  │  NEW Optimized Methods:                                   │ │ │
│  │  │  - getUsersList()          → Aggregation-based list      │ │ │
│  │  │  - getUserOverview()       → User + dates                │ │ │
│  │  │  - getUserDateMetrics()    → Specific date               │ │ │
│  │  │  - getMetricsDateRange()   → Range with projections      │ │ │
│  │  │                                                            │ │ │
│  │  │  Business Logic:                                          │ │ │
│  │  │  • Engagement scoring algorithm                           │ │ │
│  │  │  • Status determination (active/declining/inactive)       │ │ │
│  │  │  • Metrics aggregation                                    │ │ │
│  │  │  • Date range calculations                                │ │ │
│  │  └──────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
│                               │                                      │
│  ┌────────────────────────────▼──────────────────────────────────┐ │
│  │                      DATA MODEL                                │ │
│  │  ┌──────────────────────────────────────────────────────────┐ │ │
│  │  │  DailyEngagementMetrics.ts                                │ │ │
│  │  │                                                            │ │ │
│  │  │  Schema Fields:                                           │ │ │
│  │  │  • userId: ObjectId (ref: User)                          │ │ │
│  │  │  • date: Date (start of day)                             │ │ │
│  │  │  • deviceType: 'luna' | 'polar' | 'apple'               │ │ │
│  │  │  • hr: { hasData, dataPoints, avgHR, minHR, maxHR... }  │ │ │
│  │  │  • sleep: { hasData, sleepScore, stages... }            │ │ │
│  │  │  • activity: { hasData, steps, distance, calories... }   │ │ │
│  │  │  • spo2: { hasData, dataPoints, avgSpO2... }            │ │ │
│  │  │  • workouts: [{ type, startTime, duration... }]         │ │ │
│  │  │  • engagementScore: number (0-100)                       │ │ │
│  │  │  • metricsCollected: string[]                            │ │ │
│  │  │                                                            │ │ │
│  │  │  Indexes (Optimized):                                     │ │ │
│  │  │  ✓ { userId: 1, date: -1 }        → Timeline queries    │ │ │
│  │  │  ✓ { userId: 1, date: 1 }         → Unique (1/user/day) │ │ │
│  │  │  ✓ { date: -1 }                   → Recent activity     │ │ │
│  │  │  ✓ { engagementScore: 1 }         → Score filtering     │ │ │
│  │  │  ✓ { userId: 1, deviceType: 1, date: -1 } → Device     │ │ │
│  │  └──────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────┬──────────────────────────────────┘ │
└────────────────────────────────┼───────────────────────────────────┘
                                 │
┌────────────────────────────────▼───────────────────────────────────┐
│                       DATABASE (MongoDB)                            │
│                                                                      │
│  ┌─────────────────────┐  ┌─────────────────────────────────────┐ │
│  │ Users Collection    │  │  DailyEngagementMetrics Collection  │ │
│  │                     │  │                                      │ │
│  │ • _id               │  │  • _id                              │ │
│  │ • name              │  │  • userId (indexed)                 │ │
│  │ • email             │  │  • date (indexed)                   │ │
│  │ • role              │  │  • deviceType                       │ │
│  │ • ...               │  │  • hr: {...}                        │ │
│  │                     │  │  • sleep: {...}                     │ │
│  │ Indexes:            │  │  • activity: {...}                  │ │
│  │ • _id (default)     │  │  • spo2: {...}                     │ │
│  │                     │  │  • workouts: [...]                 │ │
│  │                     │  │  • engagementScore (indexed)        │ │
│  │                     │  │  • createdAt                        │ │
│  │                     │  │  • updatedAt                        │ │
│  │                     │  │                                      │ │
│  │                     │  │  Indexes:                            │ │
│  │                     │  │  • { userId: 1, date: -1 }         │ │
│  │                     │  │  • { userId: 1, date: 1 } (unique) │ │
│  │                     │  │  • { date: -1 }                    │ │
│  │                     │  │  • { engagementScore: 1 }          │ │
│  │                     │  │  • { userId: 1, deviceType: 1, date: -1 } │
│  └─────────────────────┘  └─────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                        DATA FLOW EXAMPLES                            │
└─────────────────────────────────────────────────────────────────────┘

1. GET User List (Optimized):
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │────▶│ Controller │────▶│   Service    │────▶│ MongoDB  │
   └──────────┘  1  └────────────┘  2  └──────────────┘  3  └──────────┘
                GET /users/list      getUsersList()      Aggregation Pipeline:
                                                          1. Sort by date
                                     Uses:               2. Group by userId
                                     • Aggregation       3. Get latest & avg
                                     • Map for lookup
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │◀────│ Controller │◀────│   Service    │◀────│ MongoDB  │
   └──────────┘  6  └────────────┘  5  └──────────────┘  4  └──────────┘
     JSON response   Format response   Map results        Return aggregated
     (10ms total)                                          data (5ms)


2. GET User Overview + Dates:
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │────▶│ Controller │────▶│   Service    │────▶│ MongoDB  │
   └──────────┘  1  └────────────┘  2  └──────────────┘  3  └──────────┘
           GET /users/:id/overview   getUserOverview()   1. Find user
                                                          2. Find metrics
                                     Uses:                  with projection
                                     • .lean()             (date, score only)
                                     • Projection
                                     • Single query
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │◀────│ Controller │◀────│   Service    │◀────│ MongoDB  │
   └──────────┘  6  └────────────┘  5  └──────────────┘  4  └──────────┘
     JSON with       Format response   Calculate stats    Return lean docs
     user + dates                      + date list        (50ms)
     (50ms total)


3. GET Specific Date Metrics:
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │────▶│ Controller │────▶│   Service    │────▶│ MongoDB  │
   └──────────┘  1  └────────────┘  2  └──────────────┘  3  └──────────┘
        GET /users/:id/          getUserDateMetrics()  findOne with
        date/:date                                      indexed query:
                                  Uses:                 { userId, date: {
                                  • Indexed query         $gte, $lte
                                  • .lean()              }}
                                  • Date range
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │◀────│ Controller │◀────│   Service    │◀────│ MongoDB  │
   └──────────┘  6  └────────────┘  5  └──────────────┘  4  └──────────┘
     Full metrics    Handle 404        Return metric      Return document
     JSON            if null           or null            (20ms)
     (20ms total)


4. GET Date Range (for Charts):
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │────▶│ Controller │────▶│   Service    │────▶│ MongoDB  │
   └──────────┘  1  └────────────┘  2  └──────────────┘  3  └──────────┘
        GET /users/:id/range     getMetricsDateRange()  find with:
        ?startDate=...&                                  • Date range query
        endDate=...&             Uses:                   • Projection
        metricType=hr            • Projection            • Sort by date
                                 • .lean()               • Index usage
                                 • Conditional fields
   ┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
   │ Frontend │◀────│ Controller │◀────│   Service    │◀────│ MongoDB  │
   └──────────┘  6  └────────────┘  5  └──────────────┘  4  └──────────┘
     Array of        Add count         Return projected   Return array
     metrics for                       documents          (80ms for 30 days)
     charting
     (80ms total)


┌─────────────────────────────────────────────────────────────────────┐
│                    PERFORMANCE OPTIMIZATIONS                         │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────┬────────────┬──────────┬────────────────────┐
│ Technique            │ Applied To │ Gain     │ How                │
├──────────────────────┼────────────┼──────────┼────────────────────┤
│ Aggregation Pipeline │ User List  │ 30x      │ Single DB roundtrip│
│ Lean Queries         │ All reads  │ 40%      │ Plain JS objects   │
│ Field Projection     │ Ranges     │ 50%      │ Less data transfer │
│ Compound Indexes     │ All queries│ 80%      │ Index-only scans   │
│ Date Range Indexes   │ Timelines  │ 70%      │ Sorted index scan  │
└──────────────────────┴────────────┴──────────┴────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│                    RECOMMENDED FUTURE ADDITIONS                      │
└─────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  Redis Cache    │
                    │  (Future)       │
                    └────────┬────────┘
                             │
                    Cache hot data:
                    • User lists (5 min)
                    • Overviews (10 min)
                    • Stats (5 min)
                             │
          ┌──────────────────┴──────────────────┐
          │                                      │
    On Cache Miss                          On Cache Hit
          │                                      │
          ▼                                      │
    ┌─────────────┐                             │
    │  MongoDB    │                             │
    └─────┬───────┘                             │
          │                                      │
          └──────────────┬───────────────────────┘
                         │
                    Store in cache
                    for next request
                         │
                         ▼
                  Return to client


Expected Cache Performance:
┌──────────────────┬──────────┬──────────┬─────────┐
│ Endpoint         │ No Cache │ Cached   │ Speedup │
├──────────────────┼──────────┼──────────┼─────────┤
│ /users/list      │ 10ms     │ 2ms      │ 5x      │
│ /:id/overview    │ 50ms     │ 3ms      │ 16x     │
│ /stats           │ 200ms    │ 5ms      │ 40x     │
└──────────────────┴──────────┴──────────┴─────────┘
